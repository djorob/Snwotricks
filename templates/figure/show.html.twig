{% extends 'base.html.twig' %}

{% block body %}

<section id="figure">

  <h2>{{figure.Nom}}</h2>
  <div class="row">
    <div class="col s12 m7">
      <div class="card">
        <div class="card-image">
          <img src="images/sample-1.jpg">
          <span class="card-title">Card Title</span>
        </div>
        <div class="card-content">
          <img src="{{figure.LienPhoto}}">
          {{figure.Description | raw}}
        </div>
        <div class="card-action">

          <a href="{{path('figure_edit', {'id':figure.id}) }}">mettre à jour </a> </p>

          <a href="{{path('figure_show', {'id':figure.id}) }}">voir la figure </a> </p>

        </div>
      </div>
    </div>
  </div>

</section>

{% for forum in comment %}
    <div class="Forum">
      <div class="row">
        <div class="col-3">

          {{forum.getUser().getNom()}} (<small> {{forum.createdAt | date('d/m/Y à H.i')}} </small>)
        </div>
        <div class="col">


          {{forum.content | raw}}

        </div>
      </div>

  {% endfor %}


  <!--ici on affichera le formulaire pour creer un commetnaires-->

 
{{ form(CommentType) }}




  {% endblock %}


